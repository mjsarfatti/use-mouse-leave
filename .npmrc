# eslint-plugin-react@7.37.5's published peerDependencies range doesn't yet
# list eslint@10, though it works correctly against it in practice. Remove
# this once eslint-plugin-react publishes a release that declares support.
legacy-peer-deps=true
